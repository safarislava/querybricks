package com.querybricks.query;

import com.querybricks.QueryPart;

/**
 * Represents a complete SQL query that can be converted to SQL statement
 * and can bind parameters to execution context.
 */
public interface Query extends QueryPart {
}
